<div class="mx-auto max-w-7xl px-4 py-8 sm:px-6 lg:px-8">
  <div class="mb-3">
    <%= link_to "< Back", habit_path(@habit), class: "text-indigo-600 hover:text-indigo-900 underline" %>
  </div>
  <div>
    <h1 class="font-bold text-3xl text-gray-900 mb-6">
      Edit Habit
    </h1>
    <%= render 'form', habit: @habit %>
  </div>
</div>
